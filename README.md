# Hartings CNC Traceability Demo

This repository now runs as a live demo with:

- React frontend in `frontend/`
- FastAPI backend in `backend/app/`
- Simulator app in `backend/simulator/`

Use [QUICKSTART.md](./QUICKSTART.md) to run all services.
